<h1 class="add">Add a new restaurant</h1>
<%= simple_form_for(@restaurant) do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :description %>
  <%= f.input :category, collection: Restaurant::CATEGORIES %>
  <%= f.submit "Create restaurant", class:"btn btn-primary" %>
<% end %>
